program prog01;
    var a,b,c;
    const d := 1;
    procedure k(f,g);
    var df;
    begin
        a := a * 5;
        b := 9 * 9;
    end;
    procedure h();
        procedure L();
        var x,y,z;
        begin
            x := y * z;
        end
    begin
        a := a * 1;
    end
    begin
        a := d / 1;
        while a <> b do
        begin
            a := d + 1;
            c := c + 1
        end
    end
